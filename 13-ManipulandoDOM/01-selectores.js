document.getElementById('id'); // Retorna el elemento con el id especificado
document.getElementsByClassName('clase'); // Retorna un arreglo con los elementos que tengan la clase especificada 
document.getElementsByTagName('etiqueta'); // Retorna un arreglo con los elementos que tengan la etiqueta especificada
document.querySelector('selector'); // Retorna el primer elemento que cumpla con el selector especificado
document.querySelectorAll('selector'); // Retorna un arreglo con los elementos que cumplan con el selector especificado
document.getElementsByName('nombre'); // Retorna un arreglo con los elementos que tengan el atributo name especificado
document.getElementsByTagName('etiqueta'); // Retorna un arreglo con los elementos que tengan la etiqueta especificada


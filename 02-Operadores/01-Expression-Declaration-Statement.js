/**
 * expressions = expresiones
 * declarations = declaraciones (declarando algo de forma oral)
 * statements = declaracion (declarar algo de forma escrita)
*/


// declaration: si declaramos algo que se usara en el futuro 
/**
 * let -> variable 
const -> constante
function -> funcion
'function *' 
async function -> funcion asincrona
'async function *'
class ->clase 
export 
import 

 */

//Statement : declaracion de algo que se va a usar instantaneamente
/**
 * if
 * else
 * for 
 * switch
 * while
 */
// Expression :cualquier linea que evalue un valor cada que el codigo haga algo que puede devolver un valor
/**
 *  4 + 6 -> 10 
 *  x -> 4 asignar valor  
 *  funcion() instanciar llamar funcion
 */


